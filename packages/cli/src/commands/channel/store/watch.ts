/**
 * Channel events.jsonl watcher — CLI compat re-export.
 *
 * The implementation lives in `core/channel/internal/store/watch.ts`. This
 * module keeps `commands/channel/*` import paths stable.
 */

export * from "../../../core/channel/internal/store/watch.js";
